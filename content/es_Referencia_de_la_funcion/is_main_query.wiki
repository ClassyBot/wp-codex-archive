==Descripción==
<code>is_main_query()</code> Es una función [[Conditional Tags|'''función condicional''']] que es usada para evaluar si la consulta actual (tal como dentro de un bucle/ciclo) es la "principal" consulta (en contraposición a una consulta secundaria).